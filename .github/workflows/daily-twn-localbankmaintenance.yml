name: "Daily Maintenance — 台湾本地銀行轉賠 (20:00–09:00 MYT)"

on:
  schedule:
    # Post 5 minutes before 20:00 MYT start.
    # 20:00 MYT = 12:00 UTC (same day), so we post at 11:55 UTC (previous day) daily.
    - cron: "55 11 * * *"
  workflow_dispatch:

jobs:
  domestic-bank-maintenance:
    runs-on: ubuntu-latest

    env:
      # Secrets must be configured in the repository settings
      PAGE_ID: ${{ secrets.STATUSPAGE_PAGE_ID }}
      API_KEY: ${{ secrets.STATUSPAGE_API_KEY }}

      # Title & description
      MAINTENANCE_NAME: "台湾本地銀行轉賠"
      MAINTENANCE_BODY: "お知らせ：台湾本地銀行轉賠は毎日 20:00〜09:00（GMT+8）に定期メンテナンスを実施します。"

      # Component for this maintenance (Local Taiwan Bank Transfer)
      COMPONENT_CODE: "j4p07yl30yyb"

    steps:
      - name: Compute scheduled_for and scheduled_until (UTC) — next 20:00→09:00 MYT
        id: times
        shell: bash
        run: |
          set -euo pipefail

          # Compute START (next 20:00 MYT) in UTC ISO 8601
          START=$(python3 -c 'from datetime import datetime, time, timedelta; from zoneinfo import ZoneInfo; myt=ZoneInfo("Asia/Taipei"); utc=ZoneInfo("UTC"); now=datetime.now(myt); today_start=datetime.combine(now.date(), time(20,0), tzinfo=myt); start_local=today_start if now < today_start else today_start + timedelta(days=1); print(start_local.astimezone(utc).strftime("%Y-%m-%dT%H:%M:%SZ"))')

          # Compute END (START + 11h) in UTC ISO 8601
          END=$(python3 -c 'from datetime import datetime, time, timedelta; from zoneinfo import ZoneInfo; myt=ZoneInfo("Asia/Taipei"); utc=ZoneInfo("UTC"); now=datetime.now(myt); today_start=datetime.combine(now.date(), time(20,0), tzinfo=myt); start_local=today_start if now < today_start else today_start + timedelta(days=1); end_local=start_local + timedelta(hours=11); print(end_local.astimezone(utc).strftime("%Y-%m-%dT%H:%M:%SZ"))')

          echo "start=$START" >> "$GITHUB_OUTPUT"
          echo "end=$END"     >> "$GITHUB_OUTPUT"

          echo "台湾本地銀行轉賠 UTC window: $START → $END"

      - name: Pre-check (start/end are in the future and valid)
        shell: bash
        env:
          START: ${{ steps.times.outputs.start }}
          END:   ${{ steps.times.outputs.end }}
        run: |
          set -euo pipefail
          echo "START: $START"
          echo "END:   $END"

          NOW_UTC=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

          to_epoch() { date -u -d "$1" +"%s"; }
          START_EPOCH=$(to_epoch "$START")
          END_EPOCH=$(to_epoch "$END")
          NOW_EPOCH=$(to_epoch "$NOW_UTC")

          if [ "$END_EPOCH" -le "$START_EPOCH" ]; then
            echo "::error ::END must be later than START."
            exit 1
          fi

          if [ "$START_EPOCH" -le "$NOW_EPOCH" ]; then
            echo "::error ::START must be in the future. START=$START NOW=$NOW_UTC"
            exit 1
          fi

          echo "Pre-check OK."

      - name: Create scheduled maintenance via Statuspage API
        shell: bash
        env:
          START: ${{ steps.times.outputs.start }}
          END:   ${{ steps.times.outputs.end }}
        run: |
          set -euo pipefail
          echo "Scheduling 台湾本地銀行轉賠 ${START} -> ${END} (UTC) with component ${COMPONENT_CODE}"

          HTTP_CODE=$(curl -sS -o response.json -w "%{http_code}" \
            "https://api.statuspage.io/v1/pages/${PAGE_ID}/incidents" \
            -H "Authorization: OAuth ${API_KEY}" \
            -H "Content-Type: application/x-www-form-urlencoded" \
            --data-urlencode "incident[name]=${MAINTENANCE_NAME}" \
            --data-urlencode "incident[status]=scheduled" \
            --data-urlencode "incident[body]=${MAINTENANCE_BODY}" \
            --data-urlencode "incident[scheduled_for]=${START}" \
            --data-urlencode "incident[scheduled_until]=${END}" \
            --data-urlencode "incident[scheduled_auto_in_progress]=true" \
            --data-urlencode "incident[scheduled_auto_completed]=true" \
            --data-urlencode "incident[incident_updates][0][status]=scheduled" \
            --data-urlencode "incident[incident_updates][0][affected_components][0][code]=${COMPONENT_CODE}" \
            --data-urlencode "incident[incident_updates][0][affected_components][0][new_status]=under_maintenance" )

          echo "HTTP status: $HTTP_CODE"
          cat response.json || true

          if [ "$HTTP_CODE" -ge 300 ]; then
            echo "::error ::Failed to create 台湾本地銀行轉賠 maintenance. See response above."
            exit 1
          fi

      - name: Summary
        shell: bash
        env:
          START: ${{ steps.times.outputs.start }}
          END:   ${{ steps.times.outputs.end }}
        run: |
          echo "✅ 台湾本地銀行轉賠 scheduled (20:00–09:00 MYT)."
          echo "Window (UTC): ${START} -> ${END}"
          echo "Component: ${COMPONENT_CODE} (Local Taiwan Bank Transfer)"
